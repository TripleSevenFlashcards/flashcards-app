# Secret key for Flask sessions
FLASK_SECRET=some_long_random_string_here

# Admin token for /admin/create-user
ADMIN_TOKEN=another_long_random_string_here

# Database path (optional – if omitted, defaults to app.db in project root)
DB_PATH=data/app.db
